spring.application.name=demo1
spring.datasource.url=jdbc:mysql://localhost:3306/webbanhang
spring.datasource.username=root
spring.datasource.password=adebayor9
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Swagger UI Configuration
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha

# Spring Data REST Configuration
# By default, all repositories are disabled from auto-exposure via RestConfig.java
# To selectively enable repositories, comment out disableDefaultExposure() in RestConfig.java
# and use the exposure configuration for specific domain types.
#
# Alternatively, you can use these properties to configure Spring Data REST:
spring.data.rest.base-path=/api/data
#spring.data.rest.detection-strategy=annotated
